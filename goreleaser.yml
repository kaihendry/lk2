build:
  main: main.go
  binary: lk2
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64

dockers:
- image: hendry/lk2
  tag_templates:
  - '{{ .Tag }}'
  - 'v{{ .Major }}.{{ .Minor }}'
  - 'latest'
